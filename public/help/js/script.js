document.addEventListener('DOMContentLoaded', () => {
    // ヘルプ画面では、現在のページ（is-active）を強調する以外の
    // 複雑なカード編集・削除ロジックは不要なため削除します。
    
    console.log("Help page loaded.");
    
    // 必要に応じてサイドバーの挙動などをここに記述
});